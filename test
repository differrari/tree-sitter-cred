@includeC("random.h")
@includeC("ing.h")

literal heyo(){
    return "Value";
}

bool credbuild {
    redb_mod module = new_module("cred");
	
	set_name(&module, "credgen");

	set_target(&module,target_native);
	
	switch (hello) {
	    case 1: {print("1");}
		case 2: {print("22"); }
		case 3: {print("3"); }
	}
	
}

int a {
    chunk_array:int *testarr;
    
    literal ly = "like";
    
    int test = 0;
    testarr = chunk_array_create(12,sizeof(int));
    
}

void draw(int x, int y, int color, int overwrite) { int b = 0; }

c(int x) { int l; if (x) { return 0; } }
void print(){

}

int t = u + i;

enum test_enum {
    one, 
    two, 
    three  
}



int main {
    int y = a();
    int x = 1-(0-2);
    int z = x[0];
    {
        print("This is democracy manifest");
    }
    defer {
        cleanup();
    }
    defer x = 0;
    for (int i = 0; i < 10; i = i + 1){
    }
    while (0){
        print();
    }
    return 0;
    
    if (x < 100) {  } else { print("Done with x"); }
    y = y + 1;
    if y < 100 { } else if (y == 100) { print("Exactly 100 y"); }
    print();
    
    traverse_directory(s.data, recursive: true, int (literal directory, literal name){
        print("Inside lambda");
    });
}